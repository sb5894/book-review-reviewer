import {sqliteTable,text,integer,index,primaryKey} from 'drizzle-orm/sqlite-core';
export const classroom=sqliteTable('classroom',{id:integer('id').primaryKey(),ownerId:text('owner_id').notNull(),name:text('name').notNull(),enabled:integer('enabled').notNull().default(1),createdAt:integer('created_at').notNull()});
export const sessions=sqliteTable('student_sessions',{id:text('id').primaryKey(),studentNumber:integer('student_number').notNull(),expiresAt:integer('expires_at').notNull()});
export const reviews=sqliteTable('reviews',{id:text('id').primaryKey(),sessionId:text('session_id').notNull(),studentNumber:integer('student_number').notNull(),status:text('status').notNull(),resultJson:text('result_json'),photoKeys:text('photo_keys').notNull(),createdAt:integer('created_at').notNull(),handoff:text('handoff').notNull().default(''),doneIds:text('done_ids').notNull().default('[]'),teacherComment:text('teacher_comment').notNull().default('')},t=>[index('idx_reviews_session_created').on(t.sessionId,t.createdAt),index('idx_reviews_created').on(t.createdAt)]);
export const limits=sqliteTable('rate_limits',{key:text('key').notNull(),slot:integer('slot').notNull(),count:integer('count').notNull()},t=>[primaryKey({columns:[t.key,t.slot]})]);
