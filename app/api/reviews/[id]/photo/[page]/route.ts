import { authorizedReview,bucket,AppError,failure } from '@/lib/server';
export async function GET(_:Request,{params}:{params:Promise<{id:string;page:string}>}){try{const {id,page}=await params;if(!/^[0-3]$/.test(page))throw new AppError('사진을 찾을 수 없어요.',404);const row=await authorizedReview(id);const key=JSON.parse(row.photo_keys)[Number(page)];if(!key)throw new AppError('사진을 찾을 수 없어요.',404);const obj=await bucket().get(key);if(!obj)throw new AppError('사진을 찾을 수 없어요.',404);return new Response(obj.body,{headers:{'Content-Type':obj.httpMetadata?.contentType||'image/jpeg','Cache-Control':'private, no-store','X-Content-Type-Options':'nosniff','Content-Security-Policy':"default-src 'none'"}});}catch(e){return failure(e)}}
